{
  "name": "nano-banana-image-editor",
  "version": "1.0.0",
  "description": "Image editor via easemate.ai — no account, no API key",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
